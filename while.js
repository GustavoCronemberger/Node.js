const nomeDeUsuarios = ['Joana', 'Renata', 'Cleber', 'Zeca', 'Carla',] //0, 1, 2, 3, 4.

let indiceUsuario = 0

while (indiceUsuario < nomeDeUsuarios.length) {
    console.log('Olá', nomeDeUsuarios[indiceUsuario])
    indiceUsuario += 1 // igual indiceUsuario = indiceUsuario + 1
}
